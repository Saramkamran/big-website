import { defineCollection, z } from 'astro:content';

// The `insights` collection was removed along with the Insights pages.
// Astro still expects this file to export `collections`.
export const collections = {};
